---
title: "Query User Balance"
sidebarTitle: "Query User Balance"
description: ""
---

GEThttps://api.dashlyai.cc/v1/user/balanceTry itcURLPythonJavaScriptGoJavaPHPRubySwiftC#Copy
```shiki
curl --request GET \
  --url 'https://api.dashlyai.cc/v1/user/balance' \
  --header 'Authorization: Bearer <token>'

```
200 - Success200 - Unlimited Quota User200 - User Quota Query Failed200 - Used Quota Query Failed401429Copy
```shiki
{
  "success": true,
  "remain_balance": 100.0,
  "used_balance": 25.5,
  "unlimited_quota": false
}

```
Get the remaining and used balance of the current user account. This endpoint returns user-level balance information, independent of specific tokens, for viewing the overall account balance.
## [​](#authorization)Authorization

[​](#param-authorization)AuthorizationstringrequiredAll API endpoints require Bearer Token authenticationGet your API Key:Visit the [API Key Management Page](https://dashlyai.cc/console/token) to get your API KeyAdd it to the request header:Copy
```shiki
Authorization: Bearer YOUR_API_KEY

```
## [​](#endpoints)Endpoints

Copy
```shiki
GET /v1/user/balance
GET /user/balance

```

Both endpoints have the same functionality, you can use either one.
## [​](#response)Response

[​](#param-success)successbooleanWhether the request was successful
[​](#param-message)messagestringError message (only returned on failure)
[​](#param-remain-balance)remain\_balancefloatUser remaining balance (returned on success). Returns `-1` when `unlimited_quota` is `true`
[​](#param-used-balance)used\_balancefloatUser used balance (returned on success)
[​](#param-unlimited-quota)unlimited\_quotabooleanWhether the user has unlimited quota. `true` means unlimited, `false` means limited
## [​](#token-balance-vs-user-balance)Token Balance vs User Balance

| Comparison | Token Balance (`/v1/balance`) | User Balance (`/v1/user/balance`) |
| --- | --- | --- |
| Scope | Single token | Entire user account |
| Data Source | Token’s RemainQuota and UsedQuota | User’s quota and used\_quota |
| Use Case | Monitor single API Key usage | View overall account balance |
| Limited By | Token-level quota limits | User-level quota limits |

## [​](#use-cases)Use Cases

- View overall user account balance
- Set up recharge reminders and balance alerts
- Display account balance in user dashboard

**Balance Unit Information**The unit of the balance value depends on system configuration:

- **USD** - US Dollars
- **CNY** - Chinese Yuan
- **Tokens** - Token count

**Unlimited Quota User**When a user is set to unlimited quota:

- `unlimited_quota` field returns `true`
- `remain_balance` field returns `-1`
- The user has no quota restrictions and can use the API without limits
## [​](#common-errors)Common Errors

| Error Message | Cause | Solution |
| --- | --- | --- |
| No Authorization header | Authorization header not provided | Add `Authorization: Bearer sk-xxxxx` header |
| Failed to get user quota | User doesn’t exist | Check if the user associated with the token exists |
| Failed to get used quota | Database query error | Contact admin to check system status |


**Security Note**Your API Key is like a password. Keep it secure and don’t share it with others. Always use HTTPS in production.[Query Token Balance](/en/api-reference/account/token-balance)⌘I